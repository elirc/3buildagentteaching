# Copy to .env.test. Used only by `npm run test:integration`.
# Must NOT point at your development database — the suite truncates every table.
TEST_POSTGRES_PORT=5443
DATABASE_URL="postgresql://postgres:postgres@localhost:5443/agentic_education_ops_test?schema=public"
